import React from 'react';
import { Container, Typography } from '@mui/material';

const ForgotPassword = () => (
  <Container>
    <Typography variant="h4">Forgot Password</Typography>
    <Typography variant="body1">Forgot Password component coming soon...</Typography>
  </Container>
);

const ResetPassword = () => (
  <Container>
    <Typography variant="h4">Reset Password</Typography>
    <Typography variant="body1">Reset Password component coming soon...</Typography>
  </Container>
);

const CaseDetail = () => (
  <Container>
    <Typography variant="h4">Case Detail</Typography>
    <Typography variant="body1">Case Detail component coming soon...</Typography>
  </Container>
);

export default ForgotPassword;
export { ResetPassword, CaseDetail }; 
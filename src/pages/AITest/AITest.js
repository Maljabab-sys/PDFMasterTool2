import React from 'react';
import { Container, Typography } from '@mui/material';

const AITest = () => {
  return (
    <Container>
      <Typography variant="h4">AI Test</Typography>
      <Typography variant="body1">AI Test component coming soon...</Typography>
    </Container>
  );
};

export default AITest; 